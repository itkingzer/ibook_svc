<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IBOOK</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>


<body style="background-color: rgb(58,59,60);" data-bs-theme="dark">
    <%-include('navbartap')%>
    <div class="d-flex justify-content-center align-items-center mt-3">
        <div class="card justify-content-center" style="width: 40rem;">
            <div class="card-header">
                EDIT_POST
              </div>
            <div class="card-body  p-5">
                <form method="POST" action="/EDIT/<%=posts._id%>/Update" enctype="multipart/form-data">
                    <div class="mb-3">
                        <img src="/asset/img/post_picture/<%= posts.img %>" id="post_picture" style="cursor: pointer; width: 100%; height: 20rem; object-fit: contain;" alt="Post Picture">
                        <input type="file" name="upload_img" id="post_Upload" style="display: none;" accept="image/*">
                    </div>
                    <div class="mb-3">
                        <label for="title" class="col-form-label">Title:</label>
                        <input type="text" class="form-control" name="title" name="title" value="<%= posts.title %>">
                    </div>
                    <div class="mb-3">
                        <label for="description" class="col-form-label">Message:</label>
                        <textarea class="form-control" cols="30" rows="4" name="description"><%= posts.description %></textarea>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-primary w-100">UPDATE POST</button>
                    </div>
                </form>
                
            </div>
        </div>
    </div>

</body>
</html>

<script>
    // เมื่อคลิกที่รูป ให้เปิด input file
    document.getElementById('post_picture').addEventListener('click', () => {
      document.getElementById('post_Upload').click();
    });
    
    // เมื่อเลือกไฟล์ใหม่
    document.getElementById('post_Upload').addEventListener('change', (event) => {
      const file = event.target.files[0];
      if (file) {
        // แสดงภาพที่เลือกไว้ทันทีโดยใช้ FileReader
        const reader = new FileReader();
        reader.onload = (e) => {
          document.getElementById('post_picture').src = e.target.result;
        };
        reader.readAsDataURL(file);
      }
    });
    </script>
    